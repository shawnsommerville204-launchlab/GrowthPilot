"use client";
/* eslint-disable @next/next/no-html-link-for-pages */
import { AuditResult } from "@/lib/analyzer";

const labels: [keyof AuditResult["categories"], string][] = [["website", "Website"], ["localSeo", "Local SEO"], ["reviews", "Reviews"], ["leadGeneration", "Lead generation"], ["content", "Content"], ["offer", "Offer"]];
export default function Report({ result, businessName }: { result: AuditResult; businessName: string }) {
  return <main className="report-page"><header className="report-nav"><a className="brand" href="/">GROWTH<span>PILOT</span></a><a href="/" className="back-link">New audit <span>↗</span></a></header><div className="report-wrap"><div className="report-heading"><span className="eyebrow">SIMULATED GROWTH AUDIT / {businessName || "YOUR BUSINESS"}</span><h1>Your growth<br /><em>signal is {result.score}.</em></h1><p>This is a directional MVP simulation based on the information provided, not a verified crawl or live data report.</p></div><section className="score-layout"><div className="score-ring" style={{ "--score": `${result.score * 3.6}deg` } as React.CSSProperties}><div><strong>{result.score}</strong><small>/ 100</small></div></div><div><span className="eyebrow">THE READOUT</span><h2>Meaningful room to generate more leads.</h2><p>Your foundation has something to build on. The fastest wins are concentrated in the areas below.</p></div></section><section className="section-block"><div className="section-label"><span>01</span><h2>Where you stand</h2></div><div className="category-grid">{labels.map(([key, label]) => <div className="category" key={key}><div><span>{label}</span><strong>{result.categories[key]}</strong></div><div className="bar"><i style={{ width: `${result.categories[key]}%` }} /></div></div>)}</div></section><section className="opportunity"><div><span className="eyebrow">BIGGEST OPPORTUNITY / {result.priority.impact.toUpperCase()}</span><h2>{result.priority.category}<br /><em>{result.priority.title}</em></h2><p>{result.priority.description}</p></div><button className="button button-dark">Show me how to fix this <span>↗</span></button></section><section className="two-column"><div className="section-block"><div className="section-label"><span>02</span><h2>Already working</h2></div><ul className="strength-list">{result.strengths.map((strength) => <li key={strength}><span>+</span>{strength}</li>)}</ul></div><div className="section-block"><div className="section-label"><span>03</span><h2>Priority moves</h2></div><div className="opportunity-list">{result.opportunities.map((item) => { const [title, description] = item.split("|"); return <div className="opportunity-row" key={title}><span className="priority-dot">HIGH</span><div><strong>{title}</strong><p>{description}</p></div></div>; })}</div></div></section><section className="section-block action-section"><div className="section-label"><span>04</span><h2>Your next 7 days</h2></div><div className="action-grid">{result.actions.map((action, index) => <div className="action" key={action.title}><span>DAY {index + 1}</span><strong>{action.title}</strong><p>{action.description}</p></div>)}</div></section><section className="conversion"><span className="eyebrow">READY WHEN YOU ARE</span><h2>Want us to fix it?</h2><p>GrowthPilot can implement these improvements for you, starting at <strong>$299</strong>.</p><div className="conversion-actions"><a className="button button-light" href="mailto:hello@growthpilot.co?subject=Get my business fixed">Get my business fixed <span>↗</span></a><a className="text-link" href="mailto:hello@growthpilot.co?subject=Book a growth call">Book a growth call</a></div><small>Website conversion · Local SEO · Lead capture · Review strategy · Content recommendations</small></section><footer className="report-footer"><span>GROWTH<span>PILOT</span></span><span>Built for businesses ready to be found.</span></footer></div></main>;
}
